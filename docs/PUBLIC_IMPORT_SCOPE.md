# Public Import Scope

This branch receives a clean, reviewed export of Daily Business Agent source code.

The import must not include private Git history, production configuration, credentials, certificates, customer data, fixed-egress deployment files, or release binaries.

All imported source and tests must use public-safe defaults and synthetic data.
