# 隐私说明

Daily Business Agent 采用本地优先设计。本说明描述公开源码版本的数据边界，不代替用户所属组织的内部合规要求。

## 默认本地处理

用户选择的经营报表、产品映射、库存文件和计划文件在本机处理。程序在本机任务目录生成 Excel、JSON 和 HTML 结果。

公开源码中没有经营数据遥测、广告分析 SDK 或把报表上传到项目维护者服务器的功能。

本地服务默认只监听：

```text
127.0.0.1:8766
```

## 本地保存内容

默认数据目录为：

```text
%LOCALAPPDATA%\DailyBusinessAgent
```

可能保存：

- Agent Token；
- 用户提交的任务文件；
- 任务状态和本地日志；
- 生成的报表与看板；
- 最近一次成功获取的领星店铺列表；
- 使用 Windows 当前用户 DPAPI 加密的领星连接配置。

卸载程序不会自动删除该目录。

## 领星可选连接

只有用户主动配置领星连接时，程序才会向领星相关接口发起请求。AppID、AppSecret 和固定出口连接信息在 Windows 当前用户上下文中加密保存。

公开状态接口、页面和错误消息不得返回：

- AppSecret；
- 完整 AppID；
- 固定出口用户名或密码；
- 完整代理/TLS 地址；
- Access Token 或 Refresh Token。

断开领星连接会删除本地加密凭据，但保留最近一次成功同步的店铺数据，方便用户查看历史状态。

## 固定出口边界

公开仓库只包含本地客户端和连接校验逻辑，不包含：

- 固定出口服务器部署文件；
- 真实服务器地址；
- 代理账号或密码；
- TLS 私钥或证书；
- 生产证书指纹；
- 领星真实 AppID 或 AppSecret。

这些值必须由使用者在自己的环境中安全配置。

## 日志和错误

程序会对已知凭据字段、Token 和带认证信息的代理地址进行脱敏。公开 Issue、截图或日志附件中仍可能包含用户自己的文件名、商品名或业务数据，提交前必须人工检查并删除敏感内容。

## 用户控制

用户可以：

- 不配置领星，仅使用离线报表功能；
- 关闭登录自启动；
- 断开领星连接；
- 卸载应用；
- 在备份后手动清理本地数据目录。

安全问题请按 [SECURITY.md](SECURITY.md) 私下报告，不要在公开 Issue 中提交凭据或真实经营数据。
